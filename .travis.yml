language: cpp
compiler:
  - gcc
  - clang
before_script:
  - mkdir ../build
  - cd ../build
script:
  - cmake ../StackQueue
  - make -j
  - ls
  - ./test/StackQueue_Test
notifications:
  email: false
